import Process from "@/components/shared/Process";
import { studioSteps } from "@/data/data";
import React from "react";

const StudioProcess = () => {
  return <Process data={studioSteps} />;
};

export default StudioProcess;

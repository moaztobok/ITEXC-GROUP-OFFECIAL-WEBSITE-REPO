import Process from "@/components/shared/Process";
import { erpIntegrationProcess } from "@/data/data";
import React from "react";

const ErpProcess = () => {
  return <Process data={erpIntegrationProcess} />;
};

export default ErpProcess;

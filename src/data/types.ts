export type CarouselData = {
  id: string;
  title: string;
  description: string;
  image: string;
  url: string;
};
export type CustomerCard = {
  title: string;
  description: string;
  url: string;
  image: string;
}